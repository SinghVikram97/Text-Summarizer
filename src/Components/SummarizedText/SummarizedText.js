import React, { Component } from 'react'

export default class SummarizedText extends Component {
  render() {
    return (
      <div>
        <p>
          {this.props}
        </p>
      </div>
    )
  }
}
